version: "3.9"

services:
  churn-dashboard:
    build: .
    container_name: churn_dashboard
    ports:
      - "8501:8501"
    volumes:
      # Mount your data folder so you can update the Excel without rebuilding
      - ./data:/app/data
    restart: unless-stopped
